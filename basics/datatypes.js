"use strict"; 
/* treat all JS codes as newer version. 
If not written then also it runs the newer version only. 
But it is machine dependent so prefered to write it. */

// "https://archives.ecma-international.org/1996/TC39/96-002.pdf" for ECMA standards.
// "mdn" can also be used.

let age = 20
let name = "Purav"
let girlfriend = null
let cpi;

// symbol -> unique values (a Datatype)

console.log(typeof null); // object
console.log(typeof girfriend); // undefined -> Since, the value is null so it could be any from number, sring, boolean, etc.
console.log(typeof cpi) // undefined
